# Copie para .env na raiz (desenvolvimento local). Não commite o .env.

EXPO_PUBLIC_SUPABASE_URL=https://SEU_PROJETO.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sua_chave_anon_aqui

# --- Deploy na Vercel (obrigatório para login funcionar) ---
# Project → Settings → Environment Variables → Add:
#   Nome: EXPO_PUBLIC_SUPABASE_URL     Valor: https://xxx.supabase.co
#   Nome: EXPO_PUBLIC_SUPABASE_ANON_KEY   Valor: eyJhbG...
# Vercel — API NFS-e (/api/nfe/*) e boleto Sicoob (/api/boleto/*)
SUPABASE_SERVICE_ROLE_KEY=sua_service_role_key
CERT_ENCRYPTION_KEY=chave_secreta_min_16_chars_para_senha_certificado
# Opcional em app nativo: mesma URL da Vercel para APIs serverless
# EXPO_PUBLIC_NFE_API_URL=https://seu-app.vercel.app
# Cron Vercel — consulta boletos pagos no Sicoob (1x/dia no plano Hobby; use serviço externo p/ mais frequente)
CRON_SECRET=invente_uma_string_longa_e_secreta
#
# --- Painel Azoup (outro projeto Supabase — somente leitura) ---
# Credenciais do painel admin / SaaS. NÃO use EXPO_PUBLIC_*: ficam só no servidor.
AZOUP_ADMIN_SUPABASE_URL=https://SEU_PROJETO_AZOUP_ADMIN.supabase.co
AZOUP_ADMIN_SUPABASE_SERVICE_ROLE_KEY=sua_service_role_do_outro_projeto
# Stripe do Azoup (MRR líquido/bruto com cupons). Sem isso, o painel usa só o valor local.
AZOUP_STRIPE_SECRET_KEY=sk_live_ou_sk_test_do_projeto_azoup
#
# Na Vercel: Project → Settings → Environment Variables
#   EXPO_PUBLIC_SUPABASE_URL      (mesmo valor do front)
#   EXPO_PUBLIC_SUPABASE_ANON_KEY (mesmo valor do front)
#   SUPABASE_SERVICE_ROLE_KEY     (Supabase → Settings → API → service_role)
#   CERT_ENCRYPTION_KEY           (invente uma string longa, ex. 32+ caracteres)
#   AZOUP_ADMIN_SUPABASE_URL
#   AZOUP_ADMIN_SUPABASE_SERVICE_ROLE_KEY
#   AZOUP_STRIPE_SECRET_KEY
# Depois: Redeploy
#
# Certificado A1 — no Supabase SQL Editor, após rodar migration 031:
#   INSERT INTO app_runtime_config (key, value)
#   VALUES ('cert_encryption_key', 'mesma-string-da-CERT_ENCRYPTION_KEY')
#   ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value;
# (Ou salve o certificado uma vez com a API Vercel configurada — a chave é sincronizada automaticamente.)
